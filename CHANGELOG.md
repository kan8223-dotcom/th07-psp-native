# 更新履歴

このプロジェクトは開発途中です。実機未確認の修正は、その旨を明記します。

## 2026-09-01 — v0.2.0-beta（MEレンダーワーカー世代・大型更新）

コードベースをMECC統合開発線（RID30系）へ全面更新しました。PSPの第2CPU（Media Engine）を
ゲーム処理に本格投入した初の版です。**本版の実機検証はPSP-3000のみ**。PSP-1000プロファイルは
ビルド可能ですが今期未検証です。

- **MEレンダーワーカー**: 安定NORMAL弾の移動更新と頂点生成をMEが実行し、GEが直接消費します。
  SC出力とのbit一致を起動時自己検査（30ケース）で毎回検証。実機で6面完走・ボス戦走行を確認済み。
- **Item描画のME prefix / SC suffix分担**と決定論的予算器（record数×較正レート、80%閾値）。
  幽々子撃破直後の実測でAVG −1.32ms、VSync MISS 85→32/120（約35→47fps）を確認済み。
- **Item吸引移動のME更新（A1-MOVE）**: 実機で起動・自己検査合格（6経路bit一致）。
  性能効果は比較可能区間で中立であり、対象場面での採用実証はまだ取れていません（検証継続中）。
- **SC/ME使用率メーター**: XPタスクマネージャ風の履歴グラフ2枚をロゴ付近に常時表示
  （SC=フレーム時間、ME=実測busyサイクル。100%超は赤で振切表示）。実機動作確認済み。
  **今回Lトリガで表示トグルを追加（PC実装のみ・実機未確認）**。
- **起動安全化**: Item系自己検査がclean失敗した場合、Itemのみ従来SC処理へ降格して
  弾ME・ゲーム起動を継続します（XMBへ戻る強制再起動を廃止）。実機確認済み。
- **MEキャッシュ安定化**: 連続ジョブ間のstale読みを排除するため、Item入力は
  MEのuncached alias読みへ変更。実機確認済み。
- 同梱: 実証済みGEラッパー `ge4wrap_texv1.prx`（SHA256はMakefile内に記載、ビルド時に検証）、
  メモステ操作ツール `tools/psp_stick.py`、原作データ混入ガード
  `tools/check_no_original_assets.py`、開発経緯ドキュメント `docs/devlog/`。
- ビルド: `make psp3000-a1-item-motion-build`（MECCはCMakeで自動ビルド）。
  原作の `th07.dat` / `thbgm.dat` 等は従来どおり各自の所有データを使用してください。
  本リポジトリに原作データは一切含まれません（ガードで機械検証済み）。
>>>>>>> 52e0783 (feat: v0.2.0-beta — ME render worker era (bullet/item ME offload, SC/ME usage meter with L-toggle, safe-demotion boot))

## 2026-08-27 — v0.1.7-beta（ARK-5 high-memory互換性更新）

- PSP-3000 / ARK-5で64MB版を起動した際、ARK設定に`always, highmem, off`があるとゲームの
  `main()`より前に停止し、`TH07PSP_BOOT.LOG`も更新されないことを実機で確認しました。
- 既存ARK設定を丸ごと上書きせず、`homebrew, highmem, on`だけをマージするsnippetと日英手順を
  PSP-2000+配布ZIPへ追加しました。retail game全体へhigh memoryを強制しません。
- GitHub v0.1.6 PSP-2000+配布EBOOT（SHA-256
  `55dc6c2e254d1cad8c63e3965dfdd1b4059021ccecd3bd57a70bd945370bc278`）と同梱NotoSansJP fontを
  PSP-3000 / ARK-5で使用し、設定変更後の再起動試験でタイトル画面到達を確認しました。
- ゲームplay code、ME audio、GE描画、PSP-1000 profileは変更していません。

## 2026-08-23 — v0.1.6-beta（PSP-1000音声ホットフィックス）

- v0.1.5のBGM/SE共通master gain方式を廃止しました。SEが鳴っていない出力blockはBGMを
  bit単位で変更せず、再生中のSEだけを元のDirectSound gainで32bit幅のbusへ合成します。
  SEは中間s16 clipを行わず、各BGM sampleの符号付き16bit残りheadroomへ加算します。
- TH07にはTH06のような複数blockの出力queueがないため、出力thread内でMEの完了を待つ方式を
  使用せず、SEのmixとBGMへの加算をSC上で同期実行します。PSP-1000実機ログでは、SE trigger
  2595回、mix 9106 block、headroom制限70898 channel sample、SC mix＋加算は平均504 us・最大1255 usで、
  BGM入力ringのunderrunは0回でした。OPのBGM、雑魚の爆散音、ボム音がクリアになったとの実機報告を
  確認しています。
- PSP-1000/64MB版のクリーンビルドとrelease audit、PPSSPPの32MB/64MBモデルによる
  スモーク試験を通過しています。このホットフィックスの聴感確認はPSP-1000実機のみで、
  64MB実機では未確認です。

## 2026-08-23 — v0.1.5-beta（描画・音声・リプレイ修正候補）

- STARTポーズ用のフレームバッファ取込みを論理画面と同じ上から下の行順へ統一しました。
  PSP-1000で背景・敵・自機・弾が上下反転していた問題を修正し、従来は背景が全面黒だった
  64MB版も追加ヒープなしのGE直接取込みへ統一しました。PPSSPPの32MB/64MBモデルで、通常画面と
  ポーズ画面の敵・弾・自機の上下関係が一致し、青暗い背景が見えることを確認しています。
  PSP実機での再確認が必要です。
- PSP-1000で縮小したANM atlasの実格納寸法をUV計算へ反映し、半texel補正のずれによる
  1pxの縦継ぎ目を修正しました。scissorの幅・高さ指定、上下方向、反転spriteの2頂点経路も
  修正しています。PPSSPPの1面連続フレームでは、報告された背景追従の縦線が消えました。
- PSP-1000のタイトルキャッシュをv6へ更新しました。タイトルロゴ、Start等のメニュー文字、
  難易度文字を含む3 atlasは512x256で保持し、その他の縮小atlasはpremultiplied-alphaの
  area filterを使用します。キャッシュは約1.7MBとなり、旧版は自動で再生成されます。
- TH06 PSP版の多ch時ヘッドルーム処理を参考に、BGMと有効SE gainの合計から共通master gainを
  求めるリミッターを追加しました。密集SEは即時に減衰し、終了後は約1.5秒で戻るため、
  BGM/SE比を保ったままs16 hard clipを防ぎます。MEとSCフォールバックは同じ結果になります。
  数値上の最大振幅と両ビルドを確認済みで、PSP実機での聴感確認が必要です。
- PSP-1000の弾は、同時には1つしか使わないfast/normal/slow生成アニメーションVMを共通化し、
  1発あたり3452byteから2276byteへ縮小しました。64MB版の内蔵デモで最大1002発を計測したため、
  PSP-1000版も原作相当の1024発をすべて物理確保し、slot cursorと更新順を保ちます。Itemも
  原作相当の1100個をすべて物理確保し、index parityと走査順を保ちます。64MB版の内蔵デモで
  Effect同時275個も計測したため、Effectも原作相当の400個へ戻しました。Enemyは未変更であり、
  外部リプレイ同期の保証にはPSP実機での長時間確認が必要です。PPSSPPでは同じ内蔵デモの
  60～8160フレームを60フレーム間隔で比較し、32MB/64MB版の乱数、入力、プレイヤー、弾、Item、
  Enemy状態が136地点すべてで一致し、プール枯渇もないことを確認しました。
- リプレイ完走後のReplay選択再登録前にテクスチャキャッシュを整理し、登録失敗時の1回再試行と
  タイトル復帰fallbackを追加しました。幽々子撃破後にXMBへ戻る報告の安全対策で、原因確定と
  PSP実機での該当Lunaticリプレイ確認はまだです。
- `PSP/GAME/`直下の兄弟インストールから正しい`th07.dat`/`thbgm.dat`を検出できるようにしました。
  PSP-1000版と64MB版を別フォルダへ置いても原作DATを1組だけ共有できます。各ビルドの設定、スコア、
  リプレイは従来どおりEBOOTのあるフォルダへ分離されます。

## 2026-08-13 — v0.1.4-beta（PSP-1000テスター版）

- PSP-2000/3000/Go向けの64MB版と、PSP-1000専用32MB版を別々のZIPで配布します。
  PSP-1000版はメモリ制約に合わせて最大同時数などを縮小したテスター向けビルドです。
- PSP-1000向けv0.1.4-beta通常EBOOTを実機で使用し、Normalで1面から6面、幽々子撃破後の
  Ending、スタッフロール、タイトル復帰まで確認しました。Normalの妖夢撃破後の5面→6面遷移も
  確認済みです。別の実機試験では、4面以降のポーズとゲーム復帰、コンティニューを使い切った後の
  Result画面、そこからのタイトル復帰を確認しました。
- PSP-1000版v0.1.4-betaのEasy、Hard、Lunatic、Extra、Phantasmは未確認です。
- コンティニューを使い切った直後にXMBへ戻る問題を修正しました。実機ログではゲーム本体の解放後も
  PSP描画側の再利用キャッシュにテクスチャ領域が残り、断片化したヒープ上で640x480の`result.jpg`を
  RGB24へ展開できず、結果画面の登録失敗が正常終了として扱われていました。結果画面を登録する直前に
  解放済みテクスチャを実ヒープへ返し、JPEG用の連続領域を確保します。
- 結果画面の初期化が途中で失敗した場合は、リンク済みの処理要素、スコア一覧、ANM、surfaceを通常の
  削除経路で巻き戻してから一度だけ再試行します。それでも初期化できない場合は、XMBへ終了せず
  タイトル画面へ戻してログを残します。
- PPSSPPの32MBモデルで、1面から6面まで短縮遷移させた後に実際の結果画面登録経路へ入り、
  `result.jpg`の展開と結果画面表示を確認しました。さらに隔離したテストデータだけ結果画像を破損させ、
  2回の初期化失敗後も進行データを保存してタイトル画面へ復帰することを確認しました。
- PSP-1000実機で1～3面を通過後、4面でSTARTを押すとログを残さずXMBへ戻る問題を修正しました。
  従来のポーズ背景生成は、384x448のRGBA読戻し、SDL縮小面、更新用テクスチャを同時に確保し、
  約1.25MBの一時ヒープを必要としていました。4面直前の実機ログではヒープ空きが強く断片化していたため、
  RGB565フレームバッファから既存のRGB565キャプチャテクスチャへ直接転送するPSP-1000専用経路へ
  置き換えました。ポーズ時の追加ヒープは0になり、未使用だった1MBのCPU側キャプチャコピーも削除しました。
  PPSSPPの32MBモデルとPSP-1000実機で4面以降のポーズ画面とゲーム復帰を確認済みです。
- 会話立ち絵の下端がPSPの上下反転フレームバッファ境界を越え、次の画面の最上段へ1ピクセルだけ
  現れる問題を修正しました。以前の修正は立ち絵の上端をクリップしていたため、通常の立ち絵座標では
  条件が一度も成立していませんでした。実際の立ち絵下端を論理y=478で座標・UVとも補間クリップします。
  PSP-1000実機での再確認が必要です。
- PSP-1000の32MB RAM向け専用ビルドを追加しました。起動前の静的使用量を約24.5MBから約8.3MBへ減らし、
  敵・弾・Item・Effectのプールを共有4.125MB領域から面ごとに確保します。
- 原作PBGの大きなANMは、圧縮入力全体の一時コピーを作らず4KBずつ共有領域へ直接展開します。
  PSP版で不要なSDL描画リスト、GUコマンドリスト、SE PCM、リプレイ記録領域も32MB用に縮小しました。
- 5.4MBの`title01.anm`は初回だけ読み、利用者自身の原作から約1.4MBのPSP用16bitキャッシュを
  メモリースティックへ生成します。小さいメニュー文字を含むアトラスだけは原寸で保持し、縮小対象は
  premultiplied-alphaのbox filterで輪郭の黒ずみを抑えました。
- PSP-1000でStart決定後に難易度画面へ進まず、中央だけ描画された状態で停止する問題を修正しました。
  `select00.jpg`の展開時にメモリが尽きていたため、JPEGデコーダのRGB24画素を追加コピーなしで保持し、
  LCD解像度へ縮小した後に一度だけRGB565化します。PSP-2000/3000/GoのRGBA経路は変更しません。
  PPSSPPの32MBモデルで、初回キャッシュ生成と再読込の両方からStart→難易度→自機→装備→1面開始、
  診断用の強制遷移によるStage 1～8の連続登録、Stage 8後のタイトル復帰を確認しました。これは
  ExtraとPhantasmの通常プレイ、ボス戦、ゲームクリアを確認したものではありません。
- PSP-1000だけSEを半分のサンプルレートで保持していた省メモリ処理を廃止しました。代わりに全フレームを
  G.711 mu-law 8bitで保持し、従来と同じ約796KBのRAMで原作SEの周波数帯域を維持します。
  MEとSCフォールバックの両方へ同じ復号処理を追加し、PPSSPPの32MBモデルでタイトル、メニューSE、
  ゲーム登録を確認しました。BGMは変更前から44.1kHz・16bit・ステレオPCMを無圧縮で再生しており、
  PPSSPP音声ダンプでは`thbgm.dat`の入力とバイト単位で一致することを確認しました。

## 2026-08-09 — v0.1.3-beta（背景描画ホットフィックス）

- v0.1.3-betaについて、Gensokyoclubのメンバーから、Lunaticのリプレイで5面到達まで確認したとの
  報告があります。そのリプレイが終了した理由は、残機消尽による通常終了か異常終了か判別できておらず、
  6面到達とゲームクリアは未確認です。これはPSP-1000対応前の報告であり、PSP-1000版v0.1.4-betaの
  動作確認には含めません。
- v0.1.1-betaで追加したPSP向け3D背景の頂点事前変換により、1面の木が消える、地形が崩れる、
  フォグ色が不正に見える、2面以降の背景も崩れる回帰を修正しました。
- 3Dカードのモデル行列、ステージカメラ、カメラ向きカード、フォグ距離を元の正確な描画経路へ
  戻しました。スペル開始時に低優先背景層を省略する処理も撤回しています。
- PSP描画側の部分的な行列送信省略も撤回し、画面用と3D背景用の行列一式を確実に切り替えるように
  しました。背景インスタンスのZ層分類とカリング半径キャッシュは表示結果を変えないため維持しています。
- PPSSPPで修正前の1面に木が出ない状態を再現し、修正後は最適化前のPSP版と同じ木、地形、
  フォグ色へ戻ることを比較確認しました。2面の背景表示と鳥居も確認済みです。
- SELECTの固定30fpsモード、弾・敵・Effectの軽量化、4面ボス進行不能修正は維持しています。

## 2026-08-09 — v0.1.2-beta（テスターホットフィックス）

- PSP向け敵occupancy mapで、生成直後の初期ECLが別の敵を同期生成した場合に、生成中の親と同じ
  スロットを再利用して上書きする問題を修正しました。4面プリズムリバー三姉妹の遭遇会話後に
  ボス戦が始まらない進行不能はこの問題が原因でした。
- 4面実データではボス用サブルーチンの初回ECL内に敵生成命令が3回あることを確認し、スロットを
  初期ECLより前に確保するよう変更しました。空きスロット走査の軽量化は維持しています。
- PPSSPPの4面直行試験で、会話後の最初のボススペル、後続スペル、4面突破まで確認しました。
  PSP実機でも△による会話早送り後にボス戦が正常に開始することを確認済みです。
- PSP実機の4面は通常60fpsで動作することを確認しました。負荷が大きい場面や他のステージでは、
  引き続きSELECTの固定30fpsモードを安定プレイ向けに推奨します。

## 2026-08-09 — v0.1.1-beta（テスター版）

- SELECTで通常60fpsと固定30fpsを切り替えられるようにしました。固定30fpsでは描画を1回おきにし、
  その間もゲーム計算を60Hzで進めるため、ゲーム進行、入力、BGM、SEの速度は変わりません。
  モード切替入力はリプレイへ記録しません。負荷が大きい4～6面を含む安定プレイでは固定30fpsを推奨します。
- タイトル画面の背景、ロゴ、霊夢、メニュー項目が消える問題を修正しました。初回起動、デモ復帰、
  Music Room復帰の各経路をPPSSPPで確認しています。
- タイトル放置で開始したデモをボタンで中断するとクラッシュする問題を修正しました。
  自動終了とボタン中断の両方でタイトルへ戻ることをPPSSPPで確認しています。実機での連続確認は継続中です。
- 5面クリア後に6面へ進まずXMBへ戻るメモリ不足対策として、終了済みステージのリプレイ入力と
  終了時刻領域を実使用量へ縮小し、共有ゲーム資産の解放・再登録経路を整理しました。
  当時はPPSSPPで1面から6面までの累積メモリ試験を完走し、v0.1.4-betaではPSP-1000実機の
  Normalでも妖夢撃破後の5面→6面遷移を確認済みです。
- TH6 PSP版を基に、全画面surfaceの事前縮小・EDRAMキャッシュ、RGB565表示バッファ、
  swizzle可能な固定文字atlas、2頂点スプライトと遅延描画結合をTH7へ移植しました。
- 弾と軸平行Effectを、汎用4頂点変換を通さず直接2頂点の`GU_SPRITES`として描く共通経路へ変更しました。
  回転弾は従来どおり4頂点経路を使用し、表示結果を維持します。
- 3D背景のモデル変換を各quadの頂点へ事前適用し、同一のモデル行列を背景全体で共有するようにしました。
  6面の診断では1フレームの行列送信が約35～47回から12回へ減少し、4面と6面で背景表示を確認しています。
- 弾、敵、Effect、Item、自機弾の固定配列にPSP向けoccupancy mapを追加し、未使用要素を毎フレーム
  読み続ける処理を削減しました。弾の回転角キャッシュ、画面外判定、ボム後の重複走査も整理しています。
- Music Roomは、利用者自身の`th07.dat`から生成できるRGB565背景キャッシュ、段階的な曲名・コメント生成、
  文字atlas更新の一括同期、退室時の描画・音声停止順序を追加しました。入室不能と戻る操作でXMBへ終了する
  問題は修正候補版を実機配備済みです。文字アニメーション中の処理落ちは継続課題です。
- デバッグ計測版に限りPracticeの1～6面を開放しました。通常版のクリア条件と`score.dat`は変更しません。
- PSP実機はCPU 333MHz、BUS 166MHzを要求する設定のままです。

## 2026-08-08 — v0.1.0-beta

- some100/th07 portable branchを基に、PSP向けネイティブ描画、入力、ファイルI/O、BGM/SE再生を実装しました。
- TH06 PSP Nativeの構成を参照し、PSP GE、VFPU対応libGUM、333MHz動作、MECCによるPCMミキシングと
  SCフォールバックを組み込みました。
- 日本語会話、タイトル・ステージロゴ、文字atlas、640x480→480x272縮小時の文字品質を修正しました。
- タイトルデモ、画面消灯防止、ステージ遷移、スタッフロール速度、BGM/SE速度、効果音停止処理を修正しました。
- 妖夢撃破後の6面登録前に未使用テクスチャキャッシュを解放し、失敗時は部分初期化を破棄して一度だけ再試行するようにしました。
  再試行にも失敗した場合は、正常終了扱いでXMBへ戻さずタイトルへ復帰してログを残します。
- 会話立ち絵がPSP画面の最上段へ1ピクセルだけはみ出す問題に対し、立ち絵専用の座標・UVクリップを追加しました。
- PPSSPPで5面→6面→7面→8面の強制連続登録と、登録失敗時の再試行・タイトル復帰経路を確認しました。
- 4面以降の3D背景で大きく処理落ちする問題は継続課題です。
