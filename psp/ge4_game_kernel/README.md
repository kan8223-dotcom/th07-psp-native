# TH07 GE4 game sidecar

R4 loads this tiny 6.60 kernel PRX before GU initialization. It exposes only
the already-proven model/physical-size queries and the exact 2 MiB/4 MiB
aperture transitions. The sidecar accepts PSP model 3 only.
